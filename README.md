# Animal Sounds Polymorphism 
This C# console project demonstrates OOP concepts such as inheritance, method overriding, and polymorphism. The `Animal` class defines a virtual `makeSound()` method, while classes like `Cat`, `Dog`, `Eagle`, and `Bird` override it to produce different sounds.
